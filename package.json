{
  "name": "sql-learn-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"🌐前端,🚀后端\" --prefix-colors \"cyan,magenta\" \"pnpm --filter @sql-learn/frontend dev\" \"pnpm --filter @sql-learn/backend dev\"",
    "dev:frontend": "pnpm --filter @sql-learn/frontend dev",
    "dev:backend": "pnpm --filter @sql-learn/backend dev",
    "build": "pnpm --filter @sql-learn/shared-types build && pnpm --filter @sql-learn/shared-utils build && pnpm --filter @sql-learn/ui-components build && pnpm --filter @sql-learn/frontend build",
    "build:packages": "pnpm --filter @sql-learn/shared-types build && pnpm --filter @sql-learn/shared-utils build && pnpm --filter @sql-learn/ui-components build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint",
    "type-check": "pnpm --recursive type-check",
    "clean": "pnpm --recursive clean",
    "install:all": "pnpm install",
    "install:backend": "cd apps/backend && pip install -r requirements.txt",
    "setup": "pnpm install && pnpm run install:backend",
    "check:env": "node -e \"console.log('Node:', process.version); console.log('pnpm:', require('child_process').execSync('pnpm --version', {encoding: 'utf8'}).trim()); console.log('Python:', require('child_process').execSync('python --version', {encoding: 'utf8'}).trim());\"",
    "predev": "pnpm run check:env"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "concurrently": "^8.2.2",
    "eslint": "^9.32.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}